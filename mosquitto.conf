# Mosquitto MQTT Broker 配置文件
# 支持 WebSocket (用于 Web 前端) 和 TCP (用于 LabVIEW)

# 允许匿名访问（开发环境）
allow_anonymous true

# TCP 监听端口 (LabVIEW 使用)
listener 1883

# WebSocket 监听端口 (Web 前端使用)
listener 9001
protocol websockets

# 日志配置
log_dest stdout
log_type all

# 连接限制
max_connections 100
